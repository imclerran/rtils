name: Bundle Release

on:
  # Run when a release is published
  release:
    types:
      - published

jobs:
  bundle-and-release:
    name: Bundle and release library
    runs-on: ubuntu-latest
    permissions:
      contents: write # Used to upload the bundled library
    steps:
      - name: Check out the repository
        uses: actions/checkout@v4
      - name: Install Roc
        uses: roc-lang/setup-roc@30ff82b396cbeb36a662a18f5c652773efb43e72
        with:
          version: nightly-new-compiler
      - name: Bundle and release the library
        uses: imclerran/bundle-roc-library@4367823e352af51d89d089648c847f668f4974a5 # Fork with new-Roc-CLI support; tracks https://github.com/Hasnep/bundle-roc-library/pull/34
        with:
          library: package/main.roc # Path to the library's entrypoint
          token: ${{ github.token }}